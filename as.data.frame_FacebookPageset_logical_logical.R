setMethod("as.data.frame",
    signature(x = "FacebookPageset", row.names = "logical", optional = "logical"),
    function (x, row.names = NULL, optional = FALSE, ...) 
    {
        stop("need a definition for the method here")
    }
)
